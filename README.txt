Simple Samik Console.

A simple customizable console terminal library.

Requires:

SDL 1.2
OpenGL 1.5 or later.

currently only works on windows.

Usage:

Simply create a new instance of the Terminal class and a listener to it.
The listener will then receive committed string to be parsed as you see fit.